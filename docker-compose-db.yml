version: '3.4'

networks:
  health:
    driver: bridge

services:
  mongo:
    image: mongo:4.2
    networks:
      - health
    environment:
      MONGOLAB_URI: ${DATABASE_URL:-}
      MONGO_INITDB_ROOT_USERNAME: ADMIN
      MONGO_INITDB_ROOT_PASSWORD: ADMIN
    ports:
      - "27017:27017"